extends ../basicEmail2

block content
  div(class="email")
    p(class="email__name") Hi #{firstName},
    p(class="email__title") Thank you so much for applying as a Column/ Fellow.
    p(class="email__text") Unfortunately, we’re still building the network, and don’t have a place yet where your contributions will be most appreciated.