extends ../basicEmail2

block content
  div(class="email")
    p(class="email__name") Hi #{firstName},
    p(class="email__title") Congrats! Your request to use a nom de plume was approved.
    p(class="email__text") Let’s set up your profile.
    a(class="email__button", href=`${callToActionUrl}`) FINISH SIGNING UP